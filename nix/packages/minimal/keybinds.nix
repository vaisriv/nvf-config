{...}: {
    vim = {
        binds.whichKey = {
            enable = true;
            setupOpts = {
                icons.mappings = false;
            };
        };

        # keymaps = [];
    };
}
