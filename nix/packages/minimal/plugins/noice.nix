{...}: {
    vim = {
        ui.noice = {
            enable = true;
            setupOpts = {
                cmdline.view = "cmdline";
                notify.view = "mini";
            };
        };
    };
}
